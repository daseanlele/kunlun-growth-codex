# 参与贡献

提交前请运行：

```bash
npm ci
npm test
npm run build
npm run harmony:check
cargo check --manifest-path src-tauri/Cargo.toml
```

请保持变更范围清晰，不提交 API Key、证书、运行时二进制、构建产物或企业私有配置。较大功能先创建 Issue 说明使用场景、安全边界和跨平台影响。
